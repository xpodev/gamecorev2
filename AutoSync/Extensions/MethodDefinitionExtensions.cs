using Mono.Cecil;

namespace GameCore.Net.Sync.Extensions
{
    public static class MethodDefinitionExtensions
    {
        //public static MethodReference MakeGeneric(this MethodDefinition m, params TypeReference[] types)
        //{
        //    GenericInstanceMethod def = new GenericInstanceMethod(m);
        //    foreach (TypeReference type in types)
        //    {
        //        def.GenericArguments.Add(type);
        //    }
        //    return def;
        //}
    }
}
